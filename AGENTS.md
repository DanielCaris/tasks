# Agent Guide

## Running the app after changes

After modifying Swift code or project resources, automatically run:

```bash
make run
```

This command builds the project and launches the Tasks app to verify that changes work correctly.
